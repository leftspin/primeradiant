**Findings**

- **Critical** [production_extractor.ex](/Users/mike/shared-workspace/primeradiant/storage-harness/lib/primeradiant/extraction/production_extractor.ex:149) + [production_extractor_quality_test.exs](/Users/mike/shared-workspace/primeradiant/storage-harness/test/primeradiant/production_extractor_quality_test.exs:316): The “production” extractor and eval corpus are still shaped around the old convenience grammar. Extraction is driven by regexes for `Subject predicate is value` / `predicate is value`, plus mappings like `service -> service_state`, while the six eval packs mostly use that exact artificial grammar. This does not prove T321’s required messy real-world extraction and falls under the spec’s forbidden pass conditions for grammar/demo shortcuts.

- **High** [real_ingestion.ex](/Users/mike/shared-workspace/primeradiant/storage-harness/lib/primeradiant/storage_harness/real_ingestion.ex:877): Downstream evidence provenance drops the validated evidence span details. `add_evidence_refs/5` stores only `evidence_label` and `hash(label)`, even though `EvidenceRef` has `span_start`/`span_end` fields at [schemas.ex](/Users/mike/shared-workspace/primeradiant/storage-harness/lib/primeradiant/storage_harness/schemas.ex:893). T321 requires downstream proposal/authoring paths to preserve visible span-backed evidence, not just an opaque label that has to be reverse-resolved from normalized JSON.

- **High** [envelope_adapter.ex](/Users/mike/shared-workspace/primeradiant/storage-harness/lib/primeradiant/extraction/envelope_adapter.ex:115): The adapter still synthesizes an `events` item from `input.external_id`/title and the first evidence ref instead of mapping only extractor-produced atoms. That reintroduces the old RealNormalizer-style downstream compensation path and lets external IDs/provenance shape a graph atom outside the `production_extractor_v1` contract.

I could not run the independent Codex review required by the local review skill because the sandbox cannot access `/Users/mike/.codex/sessions`; I captured the failure in `scratch/review-t321-production-extractor-quality-20260517.txt`. I also tried `mix test test/primeradiant/production_extractor_quality_test.exs`, but Mix failed before tests due an `:eperm` TCP socket error in `Mix.Sync.PubSub`. Notification was attempted; the available notify wrapper exited 0 after suppressing a duplicate alert.
